language: R
dist: bionic
cache: packages

r_packages:
  - devtools

r_github_packages:
  - r-lib/covr
  - jimhester/lintr

after_success:
  - Rscript -e 'lintr::lint_package()'
  - Rscript -e 'covr::codecov()'
  - Rscript -e 'goodpractice::gp()'

after_failure:
  - pwd
  - find . | egrep "fasta"
